import { StreamContainer } from "./Stream.styled"

const Stream = () => {
    return (
        <StreamContainer>
            <div>Stream</div>
        </StreamContainer>
    );
};

export default Stream;