import { SendChatContainer } from "./SendChat.styled"

const SendChat = () => {
    return (
        <SendChatContainer>
            Send Chat
        </SendChatContainer>
    );
};

export default SendChat;